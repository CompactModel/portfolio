server {
    listen 80;
    server_name localhost;
    root /var/www/html/public;

    location / {
        try_files $uri /index.php$is_args$args;
    }

    location ~ ^/index\.php(/|$) {
        fastcgi_pass php:9000;
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $realpath_root;
    }

    location ~ \.php$ {
        return 404;
    }
}

server {
    listen 8081;
    server_name localhost;
    root /var/www/demo-sites/autoservice;

    location / {
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 8082;
    server_name localhost;
    root /var/www/demo-sites/barbershop;

    location / {
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 8083;
    server_name localhost;
    root /var/www/demo-sites/restaurant;

    location / {
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 8084;
    server_name localhost;
    root /var/www/demo-sites/cafe;

    location / {
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 8085;
    server_name localhost;
    root /var/www/demo-sites/kebab;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
